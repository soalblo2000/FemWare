local G2L = {}

local player = game:GetService("Players").LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

-- Destroy old GUI
if player.PlayerGui:FindFirstChild("FemWare") then
    player.PlayerGui.FemWare:Destroy()
end

-- ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FemWare"
ScreenGui.Parent = player.PlayerGui
ScreenGui.ResetOnSpawn = false

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.Size = UDim2.new(0,748,0,468)
MainFrame.Position = UDim2.new(0.275,0,0.218,0)
MainFrame.BackgroundColor3 = Color3.fromRGB(255,245,252)
MainFrame.BorderSizePixel = 0
Instance.new("UICorner", MainFrame)

-- Dragging
local dragging
local dragInput
local dragStart
local startPos

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

RunService.RenderStepped:Connect(function()
    if dragging and dragInput then
        local delta = dragInput.Position - dragStart
        MainFrame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

MainFrame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

-- Top Bar
local Top = Instance.new("Frame", MainFrame)
Top.Size = UDim2.new(0,683,0,44)
Top.Position = UDim2.new(0.076,0,0.013,0)
Top.BackgroundColor3 = Color3.fromRGB(255,157,239)
Instance.new("UICorner", Top)

local Top2 = Instance.new("Frame", Top)
Top2.Size = UDim2.new(0,667,0,28)
Top2.Position = UDim2.new(0.011,0,0.17,0)
Top2.BackgroundColor3 = Color3.fromRGB(255,190,242)
Instance.new("UICorner", Top2)

local Top3 = Instance.new("Frame", Top2)
Top3.Size = UDim2.new(0,641,0,19)
Top3.Position = UDim2.new(0.019,0,0.15,0)
Top3.BackgroundColor3 = Color3.fromRGB(255,217,246)
Instance.new("UICorner", Top3)

-- Close Button
local Close = Instance.new("TextButton", Top3)
Close.Size = UDim2.new(0,16,0,16)
Close.Position = UDim2.new(1,-18,0,1)
Close.BackgroundColor3 = Color3.fromRGB(255,0,0)
Close.Text = ""
Instance.new("UICorner", Close)

Close.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Minimize
local Min = Instance.new("TextButton", Top3)
Min.Size = UDim2.new(0,16,0,16)
Min.Position = UDim2.new(1,-38,0,1)
Min.BackgroundColor3 = Color3.fromRGB(255,255,0)
Min.Text = ""
Instance.new("UICorner", Min)

Min.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.RightControl then
        MainFrame.Visible = true
    end
end)

-- Textbox Frame
local TextFrame = Instance.new("Frame", MainFrame)
TextFrame.Size = UDim2.new(0,544,0,400)
TextFrame.Position = UDim2.new(0.26,0,0.12,0)
TextFrame.BackgroundColor3 = Color3.fromRGB(255,157,239)
Instance.new("UICorner", TextFrame)

local Inner = Instance.new("Frame", TextFrame)
Inner.Size = UDim2.new(0,523,0,391)
Inner.Position = UDim2.new(0.018,0,0.02,0)
Inner.BackgroundColor3 = Color3.fromRGB(255,190,242)
Instance.new("UICorner", Inner)

local TextBox = Instance.new("TextBox", Inner)
TextBox.Size = UDim2.new(0,509,0,334)
TextBox.Position = UDim2.new(0.013,0,0.018,0)
TextBox.MultiLine = true
TextBox.ClearTextOnFocus = false
TextBox.TextXAlignment = Enum.TextXAlignment.Left
TextBox.TextYAlignment = Enum.TextYAlignment.Top
TextBox.BackgroundColor3 = Color3.fromRGB(255,217,246)
Instance.new("UICorner", TextBox)

-- Execute
local Execute = Instance.new("TextButton", MainFrame)
Execute.Size = UDim2.new(0,100,0,27)
Execute.Position = UDim2.new(0.28,0,0.89,0)
Execute.Text = "Execute"
Execute.BackgroundColor3 = Color3.fromRGB(255,217,246)
Instance.new("UICorner", Execute)

Execute.MouseButton1Click:Connect(function()
    pcall(function()
        loadstring(TextBox.Text)()
    end)
end)

-- Clear
local Clear = Instance.new("TextButton", MainFrame)
Clear.Size = UDim2.new(0,100,0,27)
Clear.Position = UDim2.new(0.43,0,0.89,0)
Clear.Text = "Clear"
Clear.BackgroundColor3 = Color3.fromRGB(255,217,246)
Instance.new("UICorner", Clear)

Clear.MouseButton1Click:Connect(function()
    TextBox.Text = ""
end)

-- Scroll buttons
local Scroll = Instance.new("ScrollingFrame", MainFrame)
Scroll.Size = UDim2.new(0,120,0,350)
Scroll.Position = UDim2.new(0.03,0,0.13,0)
Scroll.BackgroundTransparency = 1
Scroll.ScrollBarThickness = 5

local Layout = Instance.new("UIListLayout", Scroll)
Layout.Padding = UDim.new(0,5)

-- GLOBAL ADD BUTTON
getgenv().AddButton = function(name, script)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(1,0,0,25)
    Button.Text = name
    Button.TextSize = 13
    Button.BackgroundColor3 = Color3.fromRGB(255,190,242)
    Button.Parent = Scroll
    Instance.new("UICorner", Button)

    Button.MouseButton1Click:Connect(function()
        pcall(function()
            loadstring(script)()
        end)
    end)

    Scroll.CanvasSize = UDim2.new(0,0,0,Layout.AbsoluteContentSize.Y + 10)
end

return ScreenGui
